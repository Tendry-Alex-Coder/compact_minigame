{
  "name": "compact-mini",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Compact Mini — collection de minigames web",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
